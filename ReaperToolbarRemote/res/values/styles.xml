<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
	<style name ="TransportButtonTheme" > 
	    
		<item name="android:layout_height">wrap_content</item>
		<item name="android:layout_width">wrap_content</item>
		<item name="android:scaleType">fitXY</item>
		<item name="android:paddingLeft">0dp</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingRight">0dp</item>
        <item name="android:paddingBottom">5dp</item>
	    <item name="android:minWidth">93dp</item>
	    <item name="android:maxWidth">140dp</item>
	    <item name="android:minHeight">93dp</item>
	    <item name="android:maxHeight">124dp</item>
	    <item name="android:onClick">sendOsc</item>
	</style>
	<style name ="ToolbarButtonTheme" > 
	    <item name="android:scaleType">fitXY</item>
		<item name="android:paddingLeft">2dp</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingRight">2dp</item>
        <item name="android:paddingBottom">5dp</item>
	    <item name="android:minHeight">100dp</item>
	    <item name="android:maxHeight">150dp</item>
	    <item name="android:minWidth">150dp</item>
	    <item name="android:maxWidth">200dp</item>
	    <item name="android:textSize">32sp</item>
		<item name="android:background">@drawable/translucent_backgound</item>
		<item name="android:src">@drawable/empty_button</item>
		
		
		<item name="android:onClick">sendOsc</item>
	</style>
</resources>
